[project]
name = "ai-xquanty"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "numpy>=2.0,<3.0",
  "pandas>=2.2,<3.0",
  "pyarrow>=17,<19",
]

[project.optional-dependencies]
dev = ["pytest>=8.3,<9.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
